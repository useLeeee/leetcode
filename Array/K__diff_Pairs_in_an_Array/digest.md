+ 字典比列表方便，数组引用或赋值未定义的index均为报index out of range的错误，字典赋值给未定义的index就可以，但引用会报keyerror
+ 仔细审题，弄清需求（虽然有时候需求可能不是很清楚）
+ bestanswer中set的用法：1、可以用和列表推导一样的语法生成set；2、set1&set2：交集，set1|set2：并集
+ bestanswer利用了太多语言特性感觉不怎么样，但受到启发想到一个思路：遍历nums对每个元素n做以下操作：查找是否在hash表中，若在则为一对，若不在将n+k放入hash表中（思路与第一题相似）
+ 一定要逻辑清晰！！！！！！